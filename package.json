{
  "name": "harnessproof",
  "version": "0.1.5",
  "private": true,
  "description": "Independent clean-profile boot evidence for DeepSeek Harness plugins.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "npm test && node --check index.js"
  },
  "engines": {
    "node": ">=24"
  },
  "license": "MIT"
}
